## 2026-09-03
- Investigated "write DUNG in LO" (founder, kanban priority queue), found LO genuinely not ready (mod-4-only arithmetic, no runtime-parameterized exported functions -- see LO/NORTHSTAR.md), founder confirmed the real pivot: "ok write it in parena and go? right? with burrow/." Real, concrete unblock the same session: BURROW's own emit_c.go gained defstruct/get-field support, closing the exact gap parena/rect_probe.prn found live on 2026-08-30. Updated and re-verified rect_probe.prn: its original struct-CONSTRUCTING version now fails with a real, honest error (construction is deliberately still unsupported by either burrow target), rewritten to the real, working slice -- a struct parameter + get-field read, scalar return -- verified burrow build -> gcc -> run, correct result. NORTHSTAR.md updated with the real, narrower current scope for Phase 2 (structs can be passed in and read, not yet constructed). (sess-20260902-2008-ed50169e)

## 2026-08-30
- feat: real k8s scaling mod added (internal/burrowgen/k8s_scaling_gen.go). Founder real-time: "write kubernetes primitives into the stdlib and write helm support into parena both in the stdlib and also into DUNG and project BURROW". PARENA gained real stdlib/k8s + stdlib/helm packages; BURROW gained real defstruct/get-field support for the Go target the same day, specifically so DUNG could use it. internal/burrowgen/k8s_scaling_gen.go: real, checked-in output of `burrow build stdlib/k8s/scaling.prn -o ...` -- a real ScalingSpec struct + ClampReplicas function (HPA-style replica clamping), verified via a real go test. Real, honest note: not wired into any live DUNG feature yet (DUNG has no k8s-facing UI today) -- available for a real future use rather than forced into one now; a second //go:generate directive on main.go documents the regenerate command. Real, honest boundary named: the full YAML-generating stdlib/k8s/k8s.prn/stdlib/helm/helm.prn packages themselves stay C-target-only (String+Arena-based, incompatible with the Go target's GC-off-safe design) -- only the scalar-only scaling.prn companion made it into DUNG. go build/go vet/go test and a real bazel build //... all clean with both burrowgen files present. (sess-20260830-0908-5ede695f)

- feat: first real PARENA mod wired into cmd/dung/main.go -- the loop closes. Founder real-time: "continue adding to DUNG's parena work, we want to emit go with burrow" -> "once we get burrow basic stuff working of course" -> "wtart rewriting it with more parena and mods". BURROW shipped a real Phase 6 (native Go emission target, emit_go.go) in response -- DUNG is the real, concrete host its own NORTHSTAR only had a named candidate for until now. internal/burrowgen/entry_gen.go is the real, checked-in output of `burrow build parena/entry.prn -o internal/burrowgen/entry_gen.go` (a //go:generate directive on main.go documents the regenerate command); cmd/dung/main.go now calls burrowgen.SplitSize/burrowgen.NextFocusIndex directly in layout()/nextFocus() -- a real Go import, no cgo/FFI boundary at all. Verified real: go build/go vet/go test clean (split_test.go now exercises the real burrow-emitted function transitively), a real bazel build //... clean (new internal/burrowgen/BUILD.bazel, cmd/dung/BUILD.bazel updated), and the real SDL2 visor binary run under Xvfb -- identical, correct rendering to before the mod was wired in, both via plain go build and the real Bazel artifact. (sess-20260830-0908-5ede695f)

- feat: real bazel build //... now succeeds end to end, artifact verified. Founder real-time: "continue DUNG ensure we have artifacts and bazel builds once we have an entrypoint." Root-caused the real, precise blocker left open in the prior entry: gazelle's #cgo comment scanner never invokes pkg-config when generating a go_repository's BUILD file, and (confirmed empirically, twice, after a first attempt at patching literal CFLAGS/LDFLAGS lines into go-sdl2's own sdl_cgo.go still didn't produce a linux copts/clinkopts branch) doesn't reliably translate them into build attributes for this package at all. Fixed by hand-authoring sdl/BUILD.bazel directly (real pkg-config --cflags/--libs sdl2 values on linux inlined) and turning gazelle generation off for that module (go_deps.gazelle_override, MODULE.bazel), applied via a real, diff-generated patch (patches/go-sdl2-build-bazel.patch; patches/go-sdl2-linux-cflags.patch kept as accurate source-level documentation of the real bug, no longer load-bearing). `bazel build //...` now completes successfully (54 actions); ran the real bazel-bin/cmd/dung/dung_/dung binary under Xvfb -- real bash via real PTY, real colored SGR prompt rendered through vterm->font->SDL2, screenshot verified, identical to the plain-go-build proof. Also attempted "try to write the entry in parena": checked BURROW's own actual v0 emitter scope first (emit_c.go/emit_c_test.go directly, not memory), then ported the two genuinely scalar decision-logic pieces already in main.go (split-size, next-focus-index) to real PARENA source (parena/entry.prn) -- burrow parse/analyze/build all clean, emitted C gcc-compiled and run against real assertions matching main.go's own behavior, all pass. Found the real, precise next boundary: parena/rect_probe.prn's defstruct parses and region-analyzes fine but burrow build fails at "unsupported top-level form (v0 only understands defn, module, export, import)" -- real, concrete scoping data fed back into BURROW/NORTHSTAR.md's own open-items list. (sess-20260830-0908-5ede695f)

- docs: correct stale "burrow build not implemented" blocker in NORTHSTAR.md/README.md/CLAUDE.md -- BURROW's own Phase 3-4 shipped same day (gcc-verified end-to-end proof), clearing DUNG's own Phase 0. (sess-20260830-0908-5ede695f)

- feat: DUNG Phase 1 -- first real code, first real proof point. cmd/dung/main.go: an SDL2 window (Go host) with a visor-style drop-down terminal pane (F12 toggle) and a real i3-primitive binary split tree (Ctrl+Shift+Enter/Ctrl+Shift+O, Alt+Arrow focus-cycling). internal/vterm, internal/pty, internal/font vendored verbatim from PITVIPER's own already-working implementations (package names/APIs kept identical). go build/go vet/go test clean across all four packages, including a new cmd/dung/split_test.go covering the split-tree logic (leaves traversal, replaceNode, nextFocus cycling/wraparound) without needing a real PTY/SDL window. Ran for real under Xvfb with a --show flag added for exactly this (headless/scripted verification, no way to send the real F12 toggle): a real bash spawned via a real PTY, its real colored SGR prompt rendered correctly through the vterm-to-font-to-SDL2 pipeline, screenshot verified, process stable, no panics. Bazel scaffold added (MODULE.bazel + per-package BUILD.bazel, bzlmod rules_go/gazelle/go_deps.from_file), iterated against three real distinct failures: CcInfo removed under Bazel 9.2.0 (fixed: rules_go 0.50.1->0.63.0, gazelle 0.39.1->0.53.0), gazelle's own go_repository_tools fetch auto-discovering the monorepo's root go.work (go 1.25.0) and needing a matching SDK (fixed: go_sdk.download bumped to 1.25.0), and a real, still-open blocker found precisely, not glossed over: go-sdl2's #cgo pkg-config directive doesn't resolve SDL.h inside Bazel's sandboxed cgo compile even though the identical code builds clean under plain `go build` -- separate, later work (wiring copts/cdeps for the sandboxed cgo action), not Phase 1's own functional proof, which is complete and verified above. Honest, named simplifications: F12 only fires while the window has focus (no X11 global hotkey yet); nextFocus is visit-order cycling, not geometric nearest-neighbor. No editor pane yet -- that's Phase 2. (sess-20260830-0908-5ede695f)

- docs: name EMILY/BACKLOG.md as DUNG's own real, standing acceptance-test file for future file-rendering work. (sess-20260825-1938-f6bd411e)

- Initial NORTHSTAR scoping pass: DUNG, the BURROW editor. Founder-corrected twice in real time -- moved out of BURROW into its own standalone, Bazel-built repo; editor domain is a ground-up port of PARENA's own stdlib/editor/*.prn, compiled via the real burrow CLI (BURROW's own real, live, flagship dogfooding consumer). Real UX foundation adopted directly from EmilyOS's own gui-v0.1-design-capture.md (tmux x i3 hybrid layout). Real blocker named: burrow build isn't implemented yet, gated on BURROW's own Phase 3-4. Scoping only, no code yet. (sess-20260825-1938-f6bd411e)

